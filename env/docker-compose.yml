services:
  ml-agents:
    build: .
    container_name: unity-ml-agents
    user: "${UID:-1000}:${GID:-1000}"
    ports:
      - "6006:6006"  # TensorBoard
      - "5004:5004"  # Unity ML-Agents
    volumes:
      - .:/code
    restart: unless-stopped